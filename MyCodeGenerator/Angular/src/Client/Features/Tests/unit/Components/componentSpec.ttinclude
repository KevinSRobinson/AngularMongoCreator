<#+ void Generate_Features_Test(Feature feature){	#>
describe("<#=feature.Name #>", function () {

        describe("<#=feature.ListComponent.ComponentName#>", function () {

            var vm;

            //instantiate module
            beforeEach(bard.appModule('app'));

            //instantiate component controller
            beforeEach(function () {
                bard.inject(this, '$componentController');
                vm = $componentController('<#=feature.ListComponent.ComponentName#>', null);
            });


            it("should be defined", function () {
                expect(vm).toBeDefined();
            });
        });

});

<#+ CreateNewFile(feature.ComponentTestFullPath,           feature.ComponentTestFileName); }  #>

