
<#+ void GeneratRoutes(Feature feature){ #>
var config = function ($stateProvider) {
    
$stateProvider
    .state('<#=feature.CamelCasePlural#>', {
        url: '/<#=feature.CamelCasePlural#>',
        templateUrl: 'src/Client/<#=feature.AngularClient.IndexTemplateUrl#>'
    }) 
};

angular.module('<#=applicationName#>').config(config);
<#+
	 CreateDirectoryFullPath(feature.FeaturePath);
	 CreateFile(clientAppPath + helper.AngularClient.FeaturePath + "/routes.js");
}
#>
